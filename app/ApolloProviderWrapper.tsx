// ApolloProviderWrapper is no longer needed with ApolloNextAppProvider in layout.tsx
export default function ApolloProviderWrapper({ children }: { children: React.ReactNode }) {
  return children;
}
